# Local LLM via Ollama (default — no API key needed)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# Optional: use OpenAI instead of Ollama
# LLM_PROVIDER=openai
# OPENAI_API_KEY=your_key_here
